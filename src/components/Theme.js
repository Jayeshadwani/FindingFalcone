
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
        mode : "light",
        palette : {
            primary : {
                main : "#202629",
                light : "#f5f8fa",
                contrastText : "#f3f3f3"
            },
            secondary : {
                main : "#FAFAFA",
            }
}})

export default theme;